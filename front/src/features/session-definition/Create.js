import React, { createDefinition } from 'react';
// import PropTypes from 'prop-types';
import {} from './redux/hooks';

export default function Create() {
  return (
    <div className="session-definition-create">
      Component content: session-definition/Create
    </div>
  );
};

Create.propTypes = {};
Create.defaultProps = {};
